<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyContacts.View.SMSView">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="7*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
        </Grid.RowDefinitions>
        <Frame Grid.Row="0" BorderColor="Blue">
            <StackLayout  Orientation="Vertical">
                <Label Text="To:"></Label>
                <StackLayout Orientation="Horizontal">
                    <Label Text="+380"></Label>
                    <Label Text="{Binding Number}"></Label>
                </StackLayout>
                <Label Text="{Binding Name}"></Label>
            </StackLayout>
        </Frame>
        <Frame Grid.Row="1">
            <Editor  Text="{Binding TextSMS}"></Editor>
        </Frame>
        <Button Grid.Row="2" Text="Sent" Command="{Binding SentSMSCommand}"></Button>
    </Grid>
</ContentPage>